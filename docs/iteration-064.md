# Iteration 064 — DeepSeek v3一美分预算可执行性

## Before

- v3增加了trace模板和定向修复消息，默认800输出Token下的8条最坏成本预留为`$0.01082088`。
- 原脚本分配smoke `$0.002`、剩余 `$0.008`；smoke可运行，但剩余7条预留为`$0.00946960`，会在任何API调用前被预算闸门拒绝。
- 因此“总上限$0.01”虽然写在计划里，命令本身并不可执行。

## Change

- CLI新增显式`--max-output-tokens`，只接受300–2000的整数。
- v3固定使用600输出Token；诊断JSON的Schema固定且远小于该限制，重试与结构化输出不被移除。
- 预算重新分配为smoke `$0.0013`、剩余7条 `$0.0087`，总硬上限仍为`$0.01`。
- 新增基于真实8条挑战输入的最坏预算测试，同时验证smoke、remaining和总和三道门槛。

## After

- 179/179项自动化测试通过。
- v3最坏预留：smoke `$0.00123928`、remaining `$0.00868560`、total `$0.00992488`。
- 三项分别低于`$0.0013`、`$0.0087`和`$0.01`。
- v3结果Artifact仍不存在，本轮没有DeepSeek调用，新增费用为0。

## Regression

- 不通过降低重试次数或放松输出校验节省预算。
- 600是输出上限而非目标长度；模型仍必须返回完整七字段JSON和五步trace。
- 真实执行仍等待新的费用批准，并继续遵守smoke成功后才运行剩余7条。
